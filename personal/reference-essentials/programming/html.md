# HTML

HTML is not case-sensitive

```
<a href="https://www.w3schools.com">This is a link</a>
```

* All HTML elements can have **attributes**
* The `href` attribute of `<a>` specifies the URL of the page the link goes to
* The `src` attribute of `<img>` specifies the path to the image to be displayed
* The `width` and `height` attributes of `<img>` provide size information for images
* The `alt` attribute of `<img>` provides an alternate text for an image
* The `style` attribute is used to add styles to an element, such as color, font, size, and more
* The `lang` attribute of the `<html>` tag declares the language of the Web page
* The `title` attribute defines some extra information about an element

```
<h1 style="font-size:60px;">Heading 1</h1>
```

## Tags

<table><thead><tr><th>Tag</th><th>Description</th><th>Attributes</th><th data-hidden>Accepted Subtags</th></tr></thead><tbody><tr><td><code>&#x3C;!DOCTYPE html></code></td><td>Marks the start of an HTML document, required</td><td>None</td><td></td></tr><tr><td><code>&#x3C;html></code></td><td>Marks the start of an HTML document after !DOCTYPE, required</td><td>None</td><td></td></tr><tr><td><code>&#x3C;head></code></td><td>Contains metadata information about an HTML page</td><td>None</td><td></td></tr><tr><td><code>&#x3C;p></code></td><td>Creates a new paragraph</td><td></td><td></td></tr><tr><td><code>&#x3C;br></code></td><td>Creates a new line break, does not require end tag.</td><td></td><td></td></tr><tr><td><code>&#x3C;a></code></td><td>Creates a new link</td><td>href: Indicates the link to direct to</td><td></td></tr><tr><td><code>&#x3C;img></code></td><td>Creates an image</td><td>src: Source image URL<br>alt: Alternate hover text<br>width: Width of the image<br>height: Height of the image</td><td></td></tr><tr><td><code>&#x3C;h1></code> - <code>&#x3C;h6></code></td><td>Creates a heading, size of 1 is biggest.</td><td></td><td></td></tr><tr><td></td><td></td><td></td><td></td></tr></tbody></table>

<br>







<br>
